# Schema for the configuration files of the form_style module.

form_style.settings:
  type: config_object
  label: 'FormStyle settings'
  mapping:
    disable_inline_form_errors:
      type: boolean
      label: 'Disable Inline Form Errors'
